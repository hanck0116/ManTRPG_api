{
  "name": "mantrpg-api",
  "version": "0.1.0",
  "description": "Token-efficient TRPG engine/API scaffold based on ManRPG V18 rule sources.",
  "type": "module",
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.5.3",
    "tsx": "^4.20.4",
    "vitest": "^2.1.9"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
